# Base URL of the FastAPI backend (default used if unset)
NEXT_PUBLIC_API_URL=http://localhost:8000
# Google OAuth Client ID (same as the API's GOOGLE_CLIENT_ID); unset hides Google sign-in
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
